Issues/improvements found:
1- The application doesn't consider the stock of the products when adding to cart, the quantity dropdown has a maximum of 5 but the price still counting.
2- The test case for sorting in the homepage doesn't work properly.
3- In the cart when you put quantity = 0 the product dissapear so It's the same action than the remove button does. So an improvement is removing the '0' in the quantity.
4- When the cart is empty the "continue shopping" button appears twice.
5- Expiry Date field doesn't validate days. You can put 55 for example.
6- CVV field should be hidden.
7- The total field inside the order history module is showing an incorrect total price.
8- In the cart you don't have details of each product (price x quantity), there is only the "subtotal".